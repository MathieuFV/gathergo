<%= render "shared/header_card",
  title: @trip.name
%>

<!-- Affichage des destinations du voyage -->
<%= render "shared/sliding_cards_section",
  title: "Destinations",
  link_path: "#",
  remaining_days: 2,
  add_path: "#",
  cards: @destinations,
  link_path: trip_destinations_path(@trip)
%>

<%= render "shared/tabbar" %>
